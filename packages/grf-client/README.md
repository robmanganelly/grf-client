# grf-ts

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build grf-ts` to build the library.

## Running unit tests

Run `nx test grf-ts` to execute the unit tests via [Vitest](https://vitest.dev/).
